_conf:
 colors: 
    title:    brown
    line_key: orange
    line_val: gold
 sort: false
Ubuntu:
  list:     dpkg -l
  pacchetti installati:  "dpkg -L request-tracker3.8" 
  purga-pkt: dpkg -P <pacchettodapurgare>
  distro:    lsb_release -a
  Distro upgrade: update-manager -d (graphical) ; do-release-upgrade -d (textual) ; requires sudo apt-get install update-manager-core 
Debian:
  ifdownup: 'sleep 5 | ifdown eth0 && ifup eth0'
  rimuovi tutto cio che c e di grafico: apt-get --purge remove liborbit2 
Gnome:
  Torvalds suggerisce questo: sudo apt-get install gnome-tweak-tool
